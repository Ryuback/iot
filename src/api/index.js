var five = require("johnny-five");//incluindo a API e suas funcionalidades
var board = new five.Board(); // criamdo o objeto do tipo placa que é o arduino

board.on("ready", function() { //ligando a leitura do arduino via JS
    //TODO:
});
